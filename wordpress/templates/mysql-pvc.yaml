apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: wordpress
  labels:
    app.kubernetes.io/instance: wordpress
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  volumeName: mysql-pv   # 👈 Add this here
  resources:
    requests:
      storage: 10Gi
